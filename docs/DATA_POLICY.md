# Data policy

`database/` is the canonical machine-readable source. Derived CSV, GeoJSON, API payloads and search indexes may be regenerated from it.

Each material fact should identify a source type and confidence. Guidebook-derived data remains evidence-backed, not automatically field-verified. Access, conservation, safety and legal details require qualified current review before publication.
