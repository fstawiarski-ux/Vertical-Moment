# Architecture

```text
Source evidence → canonical records → validation → exports/API → website, app, viewer and guidebook
```

Area folders supply local context and asset manifests. Route records link back to area, sector and source IDs without duplicating large media files.
