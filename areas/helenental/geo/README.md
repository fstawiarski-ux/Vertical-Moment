# Höllental location data

`helental.gpx` is the 21-waypoint source file used for provisional sector
placement. It contains duplicate location names; retain it as source evidence
and resolve duplicates in the sector manifests rather than editing the raw GPX.
