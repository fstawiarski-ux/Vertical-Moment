# Intake folders

Create/use one of these folders when its source package is ready:

- `beethovenwand/`
- `engelstein/`
- `siegenfelder-steinbruch/`
- `jammerwandl/`

Do not add raw drone image sets, camera originals or enormous reconstruction
caches here. Keep those external and add a manifest/link after the selected
export is known.
