# Guidebook evidence

This folder contains structured references to user-supplied guidebook photos.
The original scans remain outside the repository until their publishing rights
and storage policy are confirmed. A SHA-256 hash ties each reference back to
the exact source file without duplicating it.

`keltenkalk-img5971-5981.json` is an evidence index, not a licence to publish
the scans or reproduce their artwork.
