# Areas

Each area folder holds local context, sector manifests, permitted source references and links to canonical route IDs. The folders below are placeholders only; populate them after source review.
