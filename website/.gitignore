# dependencies
node_modules/

# next.js build output
.next/
out/

# cloudflare pages build output
.vercel/
.wrangler/

# env
.env
.env.*
!.env.example

# misc
*.log
.DS_Store

# 3D crag models.
#
# Temporarily bundled: at four walls and ~2.7 MB, LFS handles them fine and it
# gets the 3D Lab live. The R2 move stays the plan — do it as one clean change
# once the library grows past a handful of models, then restore this ignore.
# public/models/

# Synced verbatim from database/api/v1 by scripts/sync-data.mjs at
# predev/prebuild — the generated tree, not a second copy of the dataset.
public/data/v1/

# Generated from the pinned Three.js dependency by scripts/sync-data.mjs so
# Draco-compressed GLBs remain same-origin and offline-capable.
public/vendor/model-viewer/draco/

# OpenNext
.open-next

# Generated by `npm run build:sw` from src/pwa/service-worker.ts.
public/sw.js
public/sw.js.map

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
tsconfig.tsbuildinfo
