# Tools

Reusable local tools live here. They are not production services and must be reviewed before a workflow depends on them.

- `cragkit/` turns waypoint GPX inputs into structured area working files and chapter drafts.
- `../models/blender/scripts/blender_place_sectors.py` is a legacy Blender placement helper; it contains placeholder paths and must be configured before use.
