# 3D assets

`source/` holds selected source exports organised by area and sector.
`manifests/` holds reusable schemas and asset manifests.
`archive/` holds incomplete projects and material that must not enter the
production pipeline without repair or a confirmed destination.

Commit manifests and small derived files. Use Git LFS only for approved large
binary assets; keep raw image sets and reconstruction caches in the external
archive.
